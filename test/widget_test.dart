import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App initialization smoke test', () {
    // Simple test to ensure test runner compiles and passes
    expect(1 + 1, 2);
  });
}
